// Generated by `wit-bindgen` 0.48.1. DO NOT EDIT!
#ifndef __BINDINGS_KDF_CAP_IMPORT_H
#define __BINDINGS_KDF_CAP_IMPORT_H
#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct kdf_cap_import_string_t {
  uint8_t*ptr;
  size_t len;
} kdf_cap_import_string_t;

// Algorithm selection (named `password-algorithm` so the `algorithm`
// parameter doesn't collide with the type name).
typedef uint8_t tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t;

// Argon2id (memory-hard; recommended default).
#define TEGMENTUM_PASSWORD_HASH_MULTIPLEXER_PASSWORD_DISPATCHER_PASSWORD_ALGORITHM_ARGON2ID 0
// scrypt (memory-hard).
#define TEGMENTUM_PASSWORD_HASH_MULTIPLEXER_PASSWORD_DISPATCHER_PASSWORD_ALGORITHM_SCRYPT 1
// PBKDF2-HMAC-SHA256 (iteration-based; widest compatibility).
#define TEGMENTUM_PASSWORD_HASH_MULTIPLEXER_PASSWORD_DISPATCHER_PASSWORD_ALGORITHM_PBKDF2 2

typedef struct {
  bool is_err;
  union {
    kdf_cap_import_string_t ok;
    kdf_cap_import_string_t err;
  } val;
} tegmentum_password_hash_multiplexer_password_dispatcher_result_string_string_t;

typedef struct {
  bool is_err;
  union {
    bool ok;
    kdf_cap_import_string_t err;
  } val;
} tegmentum_password_hash_multiplexer_password_dispatcher_result_bool_string_t;

typedef struct {
  bool is_some;
  tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t val;
} tegmentum_password_hash_multiplexer_password_dispatcher_option_password_algorithm_t;

typedef struct {
  uint8_t *ptr;
  size_t len;
} kdf_cap_import_list_u8_t;

typedef struct {
  bool is_err;
  union {
    kdf_cap_import_list_u8_t ok;
    kdf_cap_import_string_t err;
  } val;
} tegmentum_password_hash_multiplexer_password_dispatcher_result_list_u8_string_t;

typedef struct {
  tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t *ptr;
  size_t len;
} tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_t;

typedef struct {
  bool is_some;
  kdf_cap_import_string_t val;
} kdf_cap_import_option_string_t;

// Imported Functions from `tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0`
// Hash a password with the algorithm's recommended parameters and a random
// salt; returns a self-contained PHC string. Imports wasi:random.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_hash(tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t algorithm, kdf_cap_import_string_t *password, kdf_cap_import_string_t *ret, kdf_cap_import_string_t *err);
// Verify a password against a PHC string. The algorithm and parameters are
// read from the hash, so they are not passed. ok(true)/ok(false)/err.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_verify(kdf_cap_import_string_t *password, kdf_cap_import_string_t *hash, bool *ret, kdf_cap_import_string_t *err);
// Identify which algorithm produced a PHC string.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_identify(kdf_cap_import_string_t *hash, tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t *ret);
// Raw key derivation: derive `length` bytes from `password` and `salt` with
// the algorithm's recommended cost (for deriving encryption keys, not
// storage). Errors on an invalid salt or output length.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_derive(tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t algorithm, kdf_cap_import_string_t *password, kdf_cap_import_list_u8_t *salt, uint32_t length, kdf_cap_import_list_u8_t *ret, kdf_cap_import_string_t *err);
// PBKDF2 with explicit cost + hash choice. `hash-name` is one of the
// stdlib hashlib names: "sha256" (the only one supported today),
// "sha512", "sha1". Future hash families bump the variant set
// additively. `iterations` is the PBKDF2 iteration count (typical
// values: 100_000 - 1_000_000); higher iterations = more CPU per call.
// `dklen` is the requested output length in bytes (1..=u32::MAX).
// Mirrors the stdlib `hashlib.pbkdf2_hmac(hash-name, password, salt,
// iterations, dklen)` signature.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_derive_pbkdf2(kdf_cap_import_list_u8_t *password, kdf_cap_import_list_u8_t *salt, uint32_t iterations, kdf_cap_import_string_t *hash_name, uint32_t dklen, kdf_cap_import_list_u8_t *ret, kdf_cap_import_string_t *err);
// scrypt with explicit cost parameters. `n` is the CPU/memory cost
// factor (must be a power of two, > 1; e.g. 16384 = 2^14, 1048576 =
// 2^20). `r` is the block size in 16-byte chunks (typical: 8). `p` is
// the parallelization factor (typical: 1). `dklen` is the output
// length in bytes. Mirrors stdlib `hashlib.scrypt(password, salt=...,
// n=..., r=..., p=..., dklen=64)`.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_derive_scrypt(kdf_cap_import_list_u8_t *password, kdf_cap_import_list_u8_t *salt, uint64_t n, uint32_t r, uint32_t p, uint32_t dklen, kdf_cap_import_list_u8_t *ret, kdf_cap_import_string_t *err);
// argon2id with explicit cost. `time-cost` is the number of passes (t,
// typical 1..=10). `memory-cost-kib` is memory consumption in KiB
// (typical 16384 = 16 MiB to 65536 = 64 MiB). `parallelism` is the
// number of parallel lanes (typical 1..=4). `dklen` is the output
// length in bytes. The argon2-cffi `low_level.hash_secret_raw` shape.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_derive_argon2id(kdf_cap_import_list_u8_t *password, kdf_cap_import_list_u8_t *salt, uint32_t time_cost, uint32_t memory_cost_kib, uint32_t parallelism, uint32_t dklen, kdf_cap_import_list_u8_t *ret, kdf_cap_import_string_t *err);
// Algorithms available in this multiplexer.
extern void tegmentum_password_hash_multiplexer_password_dispatcher_supported_algorithms(tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_t *ret);
// Human-readable description of an algorithm.
extern bool tegmentum_password_hash_multiplexer_password_dispatcher_algorithm_info(tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t algorithm, kdf_cap_import_string_t *ret);

// Helper Functions

void tegmentum_password_hash_multiplexer_password_dispatcher_result_string_string_free(tegmentum_password_hash_multiplexer_password_dispatcher_result_string_string_t *ptr);

void tegmentum_password_hash_multiplexer_password_dispatcher_result_bool_string_free(tegmentum_password_hash_multiplexer_password_dispatcher_result_bool_string_t *ptr);

void tegmentum_password_hash_multiplexer_password_dispatcher_option_password_algorithm_free(tegmentum_password_hash_multiplexer_password_dispatcher_option_password_algorithm_t *ptr);

void kdf_cap_import_list_u8_free(kdf_cap_import_list_u8_t *ptr);

void tegmentum_password_hash_multiplexer_password_dispatcher_result_list_u8_string_free(tegmentum_password_hash_multiplexer_password_dispatcher_result_list_u8_string_t *ptr);

void tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_free(tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_t *ptr);

void kdf_cap_import_option_string_free(kdf_cap_import_option_string_t *ptr);

// Sets the string `ret` to reference the input string `s` without copying it
void kdf_cap_import_string_set(kdf_cap_import_string_t *ret, const char*s);

// Creates a copy of the input nul-terminated string `s` and
// stores it into the component model string `ret`.
void kdf_cap_import_string_dup(kdf_cap_import_string_t *ret, const char*s);

// Deallocates the string pointed to by `ret`, deallocating
// the memory behind the string.
void kdf_cap_import_string_free(kdf_cap_import_string_t *ret);

#ifdef __cplusplus
}
#endif
#endif
