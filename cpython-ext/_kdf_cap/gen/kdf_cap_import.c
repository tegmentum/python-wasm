// Generated by `wit-bindgen` 0.48.1. DO NOT EDIT!
#include "kdf_cap_import.h"
#include <stdlib.h>
#include <string.h>

// Imported Functions from `tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0`

__attribute__((__import_module__("tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0"), __import_name__("hash")))
extern void __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_hash(int32_t, uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0"), __import_name__("verify")))
extern void __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_verify(uint8_t *, size_t, uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0"), __import_name__("identify")))
extern void __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_identify(uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0"), __import_name__("derive")))
extern void __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_derive(int32_t, uint8_t *, size_t, uint8_t *, size_t, int32_t, uint8_t *);

__attribute__((__import_module__("tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0"), __import_name__("supported-algorithms")))
extern void __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_supported_algorithms(uint8_t *);

__attribute__((__import_module__("tegmentum:password-hash-multiplexer/password-dispatcher@0.1.0"), __import_name__("algorithm-info")))
extern void __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_algorithm_info(int32_t, uint8_t *);

// Canonical ABI intrinsics

__attribute__((__weak__, __export_name__("cabi_realloc")))
void *cabi_realloc(void *ptr, size_t old_size, size_t align, size_t new_size) {
  (void) old_size;
  if (new_size == 0) return (void*) align;
  void *ret = realloc(ptr, new_size);
  if (!ret) abort();
  return ret;
}

// Helper Functions

void tegmentum_password_hash_multiplexer_password_dispatcher_result_string_string_free(tegmentum_password_hash_multiplexer_password_dispatcher_result_string_string_t *ptr) {
  if (!ptr->is_err) {
    kdf_cap_import_string_free(&ptr->val.ok);
  } else {
    kdf_cap_import_string_free(&ptr->val.err);
  }
}

void tegmentum_password_hash_multiplexer_password_dispatcher_result_bool_string_free(tegmentum_password_hash_multiplexer_password_dispatcher_result_bool_string_t *ptr) {
  if (!ptr->is_err) {
  } else {
    kdf_cap_import_string_free(&ptr->val.err);
  }
}

void tegmentum_password_hash_multiplexer_password_dispatcher_option_password_algorithm_free(tegmentum_password_hash_multiplexer_password_dispatcher_option_password_algorithm_t *ptr) {
  if (ptr->is_some) {
  }
}

void kdf_cap_import_list_u8_free(kdf_cap_import_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    uint8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void tegmentum_password_hash_multiplexer_password_dispatcher_result_list_u8_string_free(tegmentum_password_hash_multiplexer_password_dispatcher_result_list_u8_string_t *ptr) {
  if (!ptr->is_err) {
    kdf_cap_import_list_u8_free(&ptr->val.ok);
  } else {
    kdf_cap_import_string_free(&ptr->val.err);
  }
}

void tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_free(tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void kdf_cap_import_option_string_free(kdf_cap_import_option_string_t *ptr) {
  if (ptr->is_some) {
    kdf_cap_import_string_free(&ptr->val);
  }
}

void kdf_cap_import_string_set(kdf_cap_import_string_t *ret, const char*s) {
  ret->ptr = (uint8_t*) s;
  ret->len = strlen(s);
}

void kdf_cap_import_string_dup(kdf_cap_import_string_t *ret, const char*s) {
  ret->len = strlen(s);
  ret->ptr = (uint8_t*) cabi_realloc(NULL, 0, 1, ret->len * 1);
  memcpy(ret->ptr, s, ret->len * 1);
}

void kdf_cap_import_string_free(kdf_cap_import_string_t *ret) {
  if (ret->len > 0) {
    free(ret->ptr);
  }
  ret->ptr = NULL;
  ret->len = 0;
}

// Component Adapters

bool tegmentum_password_hash_multiplexer_password_dispatcher_hash(tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t algorithm, kdf_cap_import_string_t *password, kdf_cap_import_string_t *ret, kdf_cap_import_string_t *err) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(3*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_hash((int32_t) algorithm, (uint8_t *) (*password).ptr, (*password).len, ptr);
  tegmentum_password_hash_multiplexer_password_dispatcher_result_string_string_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (kdf_cap_import_string_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (kdf_cap_import_string_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
  }
  if (!result.is_err) {
    *ret = result.val.ok;
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool tegmentum_password_hash_multiplexer_password_dispatcher_verify(kdf_cap_import_string_t *password, kdf_cap_import_string_t *hash, bool *ret, kdf_cap_import_string_t *err) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(3*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_verify((uint8_t *) (*password).ptr, (*password).len, (uint8_t *) (*hash).ptr, (*hash).len, ptr);
  tegmentum_password_hash_multiplexer_password_dispatcher_result_bool_string_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (int32_t) *((uint8_t*) (ptr + sizeof(void*)));
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (kdf_cap_import_string_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
  }
  if (!result.is_err) {
    *ret = result.val.ok;
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool tegmentum_password_hash_multiplexer_password_dispatcher_identify(kdf_cap_import_string_t *hash, tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t *ret) {
  __attribute__((__aligned__(1)))
  uint8_t ret_area[2];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_identify((uint8_t *) (*hash).ptr, (*hash).len, ptr);
  tegmentum_password_hash_multiplexer_password_dispatcher_option_password_algorithm_t option;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      option.is_some = false;
      break;
    }
    case 1: {
      option.is_some = true;
      option.val = (int32_t) *((uint8_t*) (ptr + 1));
      break;
    }
  }
  *ret = option.val;
  return option.is_some;
}

bool tegmentum_password_hash_multiplexer_password_dispatcher_derive(tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t algorithm, kdf_cap_import_string_t *password, kdf_cap_import_list_u8_t *salt, uint32_t length, kdf_cap_import_list_u8_t *ret, kdf_cap_import_string_t *err) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(3*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_derive((int32_t) algorithm, (uint8_t *) (*password).ptr, (*password).len, (uint8_t *) (*salt).ptr, (*salt).len, (int32_t) (length), ptr);
  tegmentum_password_hash_multiplexer_password_dispatcher_result_list_u8_string_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (kdf_cap_import_list_u8_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (kdf_cap_import_string_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
  }
  if (!result.is_err) {
    *ret = result.val.ok;
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

void tegmentum_password_hash_multiplexer_password_dispatcher_supported_algorithms(tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_t *ret) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(2*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_supported_algorithms(ptr);
  *ret = (tegmentum_password_hash_multiplexer_password_dispatcher_list_password_algorithm_t) { (tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t*)(*((uint8_t **) (ptr + 0))), (*((size_t*) (ptr + sizeof(void*)))) };
}

bool tegmentum_password_hash_multiplexer_password_dispatcher_algorithm_info(tegmentum_password_hash_multiplexer_password_dispatcher_password_algorithm_t algorithm, kdf_cap_import_string_t *ret) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(3*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_tegmentum_password_hash_multiplexer_password_dispatcher_algorithm_info((int32_t) algorithm, ptr);
  kdf_cap_import_option_string_t option;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      option.is_some = false;
      break;
    }
    case 1: {
      option.is_some = true;
      option.val = (kdf_cap_import_string_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
  }
  *ret = option.val;
  return option.is_some;
}

// Ensure that the *_component_type.o object is linked in

extern void __component_type_object_force_link_kdf_cap_import(void);
__attribute__((used))
void __component_type_object_force_link_kdf_cap_import_public_use_in_this_compilation_unit(void) {
  __component_type_object_force_link_kdf_cap_import();
}
